# 0001: Distaut is a separate repository

Status: accepted

Distaut is maintained separately from SiteBoy because Android requires a different interface framework, lifecycle, storage system, render scheduler, performance policy, and acceleration stack.

Shared identity is expressed through effect types, recipe migrations, fixtures, and source-lineage records rather than shared browser code.
